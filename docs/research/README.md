# Research Notes

These documents record dated investigations, not runtime contracts. Earlier Markdown,
import-adapter and migration proposals are superseded by D-138/D-139 and the
[`structured-content design`](../architecture/structured-content-ir.md). Keep their source
citations and original findings as historical context; do not restore retired code from them.

The reader UI investigation is a design proposal, not an approved UI redesign. The
volatility/grid/options investigation is an independent finance note, not a Mirawind
Library requirement. Neither changes the current product specification.

# Phase 0 一致性审计

- 日期：2026-07-24
- 审计范围：`product-spec.md`、`decision-log.md`、`open-questions.md`
- 权威顺序：已接受的决策日志高于较早的产品规格和开放问题建议
- 结果：未发现需要重新询问用户的产品分叉；发现的陈旧表述均可由
  D-043～D-085 直接解决

## 已解决冲突

| 主题 | 陈旧表述 | 权威决策 | 处理 |
|---|---|---|---|
| 原文件下载 | MinerU ZIP 不得公开下载，附件使用独立开关 | D-063、D-064、D-084 | M1 上传 ZIP 是登记原文件；权限跟随图书状态 |
| 目录编辑 | M1 可调整正文顺序 | D-044 | 删除顺序编辑；只能修改 Markdown 后重新导入 |
| 页面 alias | M1 目录预览可编辑 alias | D-046 | schema 预留字段，M1 不提供 UI |
| 目录勾选 | 未区分导航与正文 | D-048 | 勾选只控制导航，正文始终发布 |
| 标题与层级 | 可能直接改写 Markdown | D-049、D-050 | 使用 `display_title` 和 `display_level` |
| 拆页 | 允许任意人工覆盖 | D-051 | M1 只允许在标题前拆页 |
| 文档树 | schema 与持久化仍标为 OPEN | D-053～D-058 | AST 临时；持久化最小 manifest；schema 独立版本化 |
| 发布事务 | 数据库与文件切换顺序含糊 | D-076、D-077 | 文件先固化、FTS 准备、SQLite 唯一指针后切换 |
| 缓存 | 仅写“允许公共缓存”，精确规则 OPEN | D-078～D-081 | 按 HTML、阅读资源、下载、私人响应和路由分别冻结 |
| 认证 | 只写 Passkey + 备用密码 | D-059～D-061、D-083 | 补充离线初始化、完全恢复和多个 Passkey |
| 后台任务 | Astro 单进程模型未覆盖大型构建 | D-082 | 同部署独立 worker + SQLite 持久化队列 |
| 导入资源 | 没有具体安全预算 | D-065～D-075 | 同步全部体积、条目、图片、路径、时间和倍率限制 |
| 权利确认 | 开放问题仍建议发布前确认 | D-062 | M1 无确认 UI，只保留发布前策略扩展点 |
| P0 状态 | OQ-001～OQ-008 仍显示未冻结 | D-043～D-085 | 改为关闭映射，仅保留技术验证和后续问题 |

## 非冲突但需在技术阶段落地

- `book.yaml` 与 `document-manifest.json` 的正式 JSON/YAML schema
- 中文一字、二字和中英文混合查询的 FTS5 PoC
- 原子发布、任务租约、缓存和下载协议的集成测试
- 代表性与压力 MinerU ZIP fixtures
- Constitution、架构文档、M1 Feature Spec、plan 和 tasks

## 审计结论

产品问题阶段可以保持关闭。后续若 schema 或 PoC 暴露无法由现有决策推导的
重大产品分叉，才按照 D-085 重新开启定向提问；普通技术取舍直接在架构和
实现计划中记录。
